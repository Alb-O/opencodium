{
  "name": "@oc-plugins/git-narration",
  "version": "0.1.0",
  "description": "OpenCode plugin that creates atomic commits with agent descriptions after each file change",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node && bun run build:types",
    "build:types": "tsc --emitDeclarationOnly --declaration --outDir ./dist",
    "test": "bun test ./src --timeout 120000",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "@oc-plugins/shared": "^0.1.0",
    "diff": "^7",
    "simple-git": "^3.30.0",
    "zod": "^3"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.0",
    "@types/bun": "^1.3.4",
    "@types/diff": "^7",
    "typescript": "^5"
  },
  "keywords": ["opencode", "plugin", "git", "commit", "narration"],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-org/oc-plugins",
    "directory": "packages/git-narration"
  }
}
